plugins: ['env']

shell: sh

tasks:
  env.foo: echo "hello"
  env.ssh:
    post:
      - "{% include 'env.foo' %}"
#      - '{{ background(''sleep 10'',''FOO'') }}'
#      - echo $FOO
#      - wait $FOO

#      -
#        weight: -10
#        exec: '{{ ''echo "finished..."'' | background(''FOO'') }}'
#      -
#        weight: -5
#        exec: echo $FOO

envs:
  testing:
    ssh: git
    root: ~