plugins: ['env']

shell: sh

tasks:
  env.ssh:
    post:
      - '{{ ''echo "finished..." && sleep 10'' | background(''FOO'') }}'
      - echo $FOO
      - wait $FOO
#      -
#        weight: -10
#        exec: '{{ ''echo "finished..."'' | background(''FOO'') }}'
#      -
#        weight: -5
#        exec: echo $FOO

envs:
  testing:
    ssh: testing
    root: ~